# AGENTS.md

This project's operating contract for any LLM agent lives in
[CLAUDE.md](CLAUDE.md). Read it first: it defines the pipeline, the lifecycle
enum, the nine invariants, the four-layer config model, and the scripts.

The slash commands in `.claude/commands/` are plain markdown and runnable by
any capable agent — read the relevant command file and follow it.
