# Generic pack roles — one open cluster, deliberately empty. /setup forks this
# pack into the workspace (packs.py --fork generic --as <domain>) and replaces
# this cluster with the user's real ones, competencies included.
clusters:
  General:
    title: General
    competencies: []
    adjacent: []
